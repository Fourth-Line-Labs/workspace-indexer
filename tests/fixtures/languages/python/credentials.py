"""A file that must NOT be indexed.

The value below is synthetic and has no provider prefix -- a real token shape
would trip the host's own push protection, as this project has already found
out once. What it exercises is the entropy rule, which is the one that decides.
"""

DATABASE_PASSWORD = "k7Fq2mZx9RtVwLpA3nBcYdQe"
